version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: meuplantao-db
    ports:
    - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: 8643
      POSTGRES_DB: MeuPlantaoDB
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - meuplantao-network
    restart: unless-stopped

  backend:
    image: zhunixy/meuplantao:1.5
    container_name: meuplantao-api
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      ASPNETCORE_URLS: http://+:8082
      ConnectionStrings__Default: Host=postgres;Port=5432;Database=MeuPlantaoDB;Username=postgres;Password=8643
      Jwt__Key: ThisIsAMuchLongerSecretKeyForJWTAuthenticationWithMoreThan256Bits123456789
      Jwt__Issuer: MeuPlantao
      Jwt__Audience: MeuPlantaoUsers
      Jwt__ExpiresInHours: 8
    ports:
      - "80:8082"
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - meuplantao-network
    restart: unless-stopped

volumes:
  postgres_data:
    driver: local

networks:
  meuplantao-network:
    driver: bridge
